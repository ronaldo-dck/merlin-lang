tome tomename {
	ingredients {
		int nome : 0?
        float naoinicializado?
		int[3] vetorexemplo?
      }

      naoinicializado : 2.102?
      summon(naoinicializado)?
      while(nome < 13){
	      if (nome / 2 = 0){
		      nome : nome/2
            }
            if(nome/2 > 0 & id) {
	            nome : nome * 3 +1?
            } ~ talvez ele pare
      } 
      echo("nome = ")?
      echo(nome)?

    for(i = 0? i < n?1)

}